import os
import socket
import time
from ping3 import ping

soc = socket.socket()
target = input("enter the ip: ")

def gethost():
    host = soc.connect(targe)
    print(host)

    print("ok") # DEBUG:


gethost()
def main():
    response = os.system("ping -c 1 " + host)

    print("ok") # DEBUG:


main()
